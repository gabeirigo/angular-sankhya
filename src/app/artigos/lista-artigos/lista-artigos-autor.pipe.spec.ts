import { ListaArtigosAutorPipe } from './lista-artigos-autor.pipe';

describe('ListaArtigosAutorPipe', () => {
  it('create an instance', () => {
    const pipe = new ListaArtigosAutorPipe();
    expect(pipe).toBeTruthy();
  });
});

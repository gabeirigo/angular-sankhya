import { ListaArtigosPipe } from './lista-artigos.pipe';

describe('ListaArtigosPipe', () => {
  it('create an instance', () => {
    const pipe = new ListaArtigosPipe();
    expect(pipe).toBeTruthy();
  });
});
